-- 로컬 저장소 생성
    1. 폴더 생성
    2. 터미널에서 git init >> 
    3. ls -al >> .git 폴더 확인

-- 버전 생성
    1. git add README.md >> 원하는 파일 선택
    2. git commit -m "프로젝트 설명 파일 추가" >> 메시지 달아 커밋
    3. git log >> 생성한 커밋 보기
    4. git add . >> 다 추가